import type { FC } from 'react';

import s from './cart-page.module.css';

const CartPage: FC = () => {
  return (
    <div className={s.container}>
      <h2>Cart page</h2>
    </div>
  );
};

export { CartPage };
