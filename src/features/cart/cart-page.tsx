import type { FC } from 'react';

import classes from './cart-page.module.css';

const CartPage: FC = () => {
  return (
    <div className={classes.container}>
      <h2>Cart page</h2>
    </div>
  );
};

export { CartPage };
